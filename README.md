# 📖 النشاط العلمي لأعضاء هيئة التدريس - قسم القراءات

موقع ويب لاستعراض وأرشفة النشاط العلمي لأعضاء هيئة التدريس بقسم القراءات.

## 🌐 الاستضافة

- **GitHub**: استضافة الملفات والبيانات
- **Netlify**: نشر الموقع تلقائياً

## 📁 هيكل المشروع

```
faculty-activities/
├── index.html              # الصفحة الرئيسية
├── app.js                  # كود JavaScript
├── style.css               # التنسيقات
├── README.md               # هذا الملف
│
└── data/                   # 📂 مجلد البيانات (ملفات موحدة مع حقل year)
    ├── config.json         # الإعدادات العامة
    ├── faculty.csv         # أعضاء هيئة التدريس (مع year)
    ├── students_count.csv  # أعداد الطلاب (مع year)
    ├── theses.csv          # الرسائل العلمية (مع year)
    ├── publications.csv    # البحوث المنشورة (مع year)
    ├── events.csv          # الفعاليات (مع year)
    └── awards.csv          # الجوائز (مع year)
```

---

## 📝 كيفية تعديل البيانات

### 1️⃣ تعديل الإعدادات العامة (`data/config.json`)

```json
{
  "current_year": 1447,           // السنة الهجرية الافتراضية
  "available_years": [1446, 1447], // السنوات الهجرية المتاحة
  "weights": {                     // أوزان النقاط (قابلة للتعديل)
    "phd_supervision": 10,
    "publication": 15,
    ...
  }
}
```

### 2️⃣ إضافة سنة جديدة

1. أنشئ مجلداً جديداً: `data/1448/`
2. انسخ ملفات CSV من سنة سابقة
3. عدّل البيانات
4. أضف السنة في `config.json`:
   ```json
   "available_years": [1446, 1447, 1448]
   ```

---

## 📋 شرح ملفات CSV

### 👥 faculty.csv (أعضاء هيئة التدريس)

| الحقل | الوصف | مثال |
|-------|-------|------|
| `id` | رقم تعريفي فريد | 4330068 |
| `year` | السنة الهجرية | 1447 |
| `name` | الاسم الكامل | د. أحمد محمد |
| `rank` | الرتبة العلمية | أستاذ / أستاذ مشارك / أستاذ مساعد |
| `email` | البريد الإلكتروني | ahmed@uqu.edu.sa |
| `active` | نشط؟ | نعم / لا |
| `department` | القسم | الثقافة الإسلامية |
| `nationality` | الجنسية | السعودية |
| `gender` | الجنس | ذكر / أنثى |
| `branch` | الفرع أو الفروع المرصود التدريس فيها | تربة أو تربة\|الخرمة |

```csv
id,year,name,rank,email,active,department,nationality,gender,branch
4330068,1447,أ. أمل محمد فهد الجودي,أستاذ مساعد,,نعم,الشريعة,السعودية,أنثى,الحوية
```

أسماء مدرسي بكالوريوس الدراسات الإسلامية في تربة ورنية والخرمة مشتقة من سجلات الشعب الرسمية للمقررات التخصصية غير المشتركة مع خطط البرامج الأخرى. لذلك تمثل القائمة حدًا أدنى موثقًا، ولا تُنسب شعبة خدمية مشتركة إلى البرنامج في غياب حقل البرنامج من المصدر. يثبت السجل التدريس الفعلي في الفرع، ولا يثبت المقر الوظيفي. رُبطت الأسماء بأرقام المنسوب من صفحات جامعة الطائف الرسمية ومن ملف استطلاعات قديم بعد التحقق من تطابق نظام الأرقام، ولا تُنشأ أرقام بديلة. تبقى أي معلومة غير مثبتة بمصدر موثوق فارغة. إذا درّس العضو في أكثر من فرع في السنة نفسها، تُفصل الفروع بالرمز `|` ويشمله مرشح كل فرع. بيانات 1448 نسخة من 1447 إلى حين توفر سجلات مستقلة لها.

تُحفظ الحالات التي يثبت فيها التدريس ولا يتوفر لها رقم منسوب موثق في `scripts/branch_faculty_unresolved.csv` ولا تُضاف إلى `faculty.csv` برقم مصطنع. عند توفر الرقم الرسمي تُنقل الحالة إلى `scripts/branch_faculty_registry.csv` ثم يُعاد تشغيل سكربت التحديث.

تُحفظ عضويات الفروع التي لا يسندها إلا مقرر خدمي مشترك في `scripts/branch_faculty_shared_courses.csv` مع ملفات المصدر وبصماتها وسبب الاستبعاد، فلا تضيع الملاحظة الخام ولا تدخل في أرقام البرنامج المؤكدة.

تُحفظ تصحيحات أرقام المنسوب الموروثة من نسخ سابقة في `scripts/faculty_identity_corrections.csv` مع مصدر الاستدلال ومستواه، ويطبقها سكربت التحديث قبل التحقق من عدم تكرار الأرقام.

---

### 🎓 students_count.csv (أعداد الطلاب)

| الحقل | الوصف |
|-------|-------|
| `program` | البرنامج (دكتوراه / ماجستير) |
| `count` | العدد |

```csv
program,count
دكتوراه,45
ماجستير,120
```

---

### 📚 theses.csv (الرسائل العلمية)

| الحقل | الوصف | القيم الممكنة |
|-------|-------|---------------|
| `id` | رقم تعريفي | 1, 2, 3... |
| `type` | نوع الرسالة | دكتوراه / ماجستير |
| `specialization` | التخصص | قراءات / دراسات قرآنية |
| `student_name` | اسم الطالب | نص |
| `title` | عنوان الرسالة | نص |
| `supervisor_id` | رقم المشرف | من faculty.csv |
| `co_supervisor_id` | رقم المشرف المشارك (اختياري) | من faculty.csv |
| `examiner1_id` | رقم المناقش الأول | من faculty.csv |
| `examiner2_id` | رقم المناقش الثاني | من faculty.csv |
| `status` | الحالة | منجزة / جارية |
| `defense_date` | تاريخ المناقشة | YYYY-MM-DD (فارغ إذا جارية) |

```csv
id,type,specialization,student_name,title,supervisor_id,co_supervisor_id,examiner1_id,examiner2_id,status,defense_date
1,دكتوراه,قراءات,سالم أحمد,أثر القراءات في التفسير,1,,2,3,منجزة,2024-03-15
```

---

### 📝 publications.csv (البحوث المنشورة)

| الحقل | الوصف | ملاحظة |
|-------|-------|--------|
| `id` | رقم تعريفي | |
| `title` | عنوان البحث | |
| `authors_ids` | أرقام المؤلفين | تفصل بـ `\|` للمتعددين |
| `journal` | اسم المجلة | |
| `publish_date` | تاريخ النشر | YYYY-MM-DD |
| `citations_range` | نطاق الاقتباسات | أقل من 10 / 11-20 / 21-50 / 51-100 / أكثر من 100 |
| `student_author` | هل المؤلف طالب؟ | نعم / لا |

```csv
id,title,authors_ids,journal,publish_date,citations_range,student_author
1,دراسة تحليلية لمنهج الداني,1,مجلة الدراسات القرآنية,2024-02-10,11-20,لا
2,القراءات الشاذة وأثرها,2|3,مجلة جامعة أم القرى,2024-04-15,أقل من 10,لا
```

> **ملاحظة**: عند وجود أكثر من مؤلف، افصل بينهم بـ `|` مثل: `1|2|3`

---

### 🎤 events.csv (الفعاليات العلمية)

| الحقل | الوصف | القيم |
|-------|-------|-------|
| `id` | رقم تعريفي | |
| `type` | نوع الفعالية | مؤتمر / ندوة / ورشة |
| `name` | اسم الفعالية | |
| `location` | المكان | |
| `date` | التاريخ | YYYY-MM-DD |
| `participant_ids` | أرقام المشاركين | تفصل بـ `\|` |
| `participation_type` | نوع المشاركة | مشاركة بورقة / مشاركة / حضور |
| `organized_by_program` | منظمة من القسم؟ | نعم / لا |

```csv
id,type,name,location,date,participant_ids,participation_type,organized_by_program
1,مؤتمر,المؤتمر الدولي للقراءات,مكة المكرمة,2024-03-10,1|2,مشاركة بورقة,لا
```

---

### 🏆 awards.csv (الجوائز وبراءات الاختراع)

| الحقل | الوصف | القيم |
|-------|-------|-------|
| `id` | رقم تعريفي | |
| `type` | النوع | جائزة / براءة اختراع |
| `name` | اسم الجائزة/البراءة | |
| `granting_body` | الجهة المانحة | |
| `recipient_id` | رقم الحاصل عليها | من faculty.csv |
| `date` | التاريخ | YYYY-MM-DD |

```csv
id,type,name,granting_body,recipient_id,date
1,جائزة,جائزة التميز البحثي,جامعة أم القرى,1,2024-04-20
```

---

## ⚖️ نظام النقاط (Leaderboard)

| النشاط | النقاط الافتراضية |
|--------|-------------------|
| الإشراف على دكتوراه | 10 |
| مناقشة دكتوراه | 5 |
| نشر بحث علمي | 15 |
| الإشراف على ماجستير | 3 |
| مناقشة ماجستير | 2 |
| مشاركة بورقة في مؤتمر | 8 |
| مشاركة في ندوة/ورشة | 5 |
| حضور فعالية | 1 |
| جائزة | 10 |
| براءة اختراع | 15 |

> يمكن تعديل الأوزان من `config.json`

---

## 🚀 النشر على Netlify

### الخطوات:

1. **ارفع المشروع على GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/USERNAME/faculty-activities.git
   git push -u origin main
   ```

2. **اربط مع Netlify**
   - اذهب إلى [netlify.com](https://netlify.com)
   - اختر "Add new site" → "Import an existing project"
   - اربط حساب GitHub واختر المستودع
   - انقر "Deploy"

3. **تحديث البيانات**
   - عدّل ملفات CSV مباشرة في GitHub
   - Netlify سينشر التحديثات تلقائياً خلال دقائق

---

## ⚠️ ملاحظات هامة

1. **تنسيق التاريخ**: استخدم دائماً `YYYY-MM-DD` (مثل: 2024-03-15)
2. **الأرقام التعريفية**: تأكد من تطابق `id` بين الملفات
3. **الفواصل**: استخدم `|` للفصل بين أرقام متعددة
4. **الترميز**: احفظ الملفات بترميز UTF-8
5. **GitHub URL**: بعد الرفع، عدّل `DATA_BASE_URL` في `app.js`:
   ```javascript
   const DATA_BASE_URL = 'https://raw.githubusercontent.com/YOUR_USERNAME/faculty-activities/main/data';
   ```

---

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل، افتح Issue في GitHub.

---

**تم التطوير بواسطة Claude AI** 🤖
